const std = @import("std");
// const zigfsm = @import("zigfsm");

pub fn main() !void {
    std.debug.print("hdr parser\n", .{});

    // const States = enum {
    //     initial,
    // };
    // const Events = enum {};

    // var fsm = zigfsm.StateMachine(States, Events, States.initial).init();
}
